Server (Express + lowdb)

Install and run:

```
cd server
npm install
npm run dev
```

API endpoints:
- GET /api/products?q=search
- GET /api/products/:id
- POST /api/products
- PUT /api/products/:id
- DELETE /api/products/:id
