import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.productsystem',
  appName: 'product-system',
  webDir: 'dist'
};

export default config;
